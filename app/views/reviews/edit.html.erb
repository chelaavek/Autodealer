<% unless current_admin_user && current_admin_user.email == user.email %>
    <% controller.redirect_to store_url %>
<% end %>
    <h1>Editing Review</h1>

<%= render 'form', review: @review %>

<%= link_to 'Show', @review %> |
<%= link_to 'Back', reviews_path %>
